node_modules
dist
*.docx
# Ausnahme: die QR-Vorlage gehoert zum Code. Sie steht neben den
# TN-Listen-Vorlagen in templates/ und wird von portal/qr.ts gefuellt --
# anders als fz.docx und best-brief-1.docx im Wurzelverzeichnis, die aus dem
# Deployment kommen und deshalb ignoriert bleiben.
!templates/*.docx
*.pdf

.yarn/*
!.yarn/cache
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
